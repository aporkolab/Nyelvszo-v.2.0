# Audit configuration
# tar vulnerability (GHSA-8qq5-rm4j-mr97) has no fix available yet (tar@7.5.3 doesn't exist)
# This is a build-time dependency from @angular/cli -> pacote -> tar
# Tracking: https://github.com/npm/node-tar/issues/434
audit-level=critical

# Allow peer dependency conflicts (ngx-bootstrap doesn't support Angular 21 yet)
legacy-peer-deps=true
